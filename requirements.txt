# Eagle IDE Python dependencies (Python 3.12+)
# Install with: pip install -r requirements.txt

Flask==3.1.3              # Web framework (HTTP routes + template serving)
Flask-SocketIO==5.6.1     # WebSocket / Socket.IO support (real-time code output)
simple-websocket==1.1.0   # WebSocket transport for Flask-SocketIO threading mode
requests==2.34.2          # HTTP client used for Ollama AI API calls
bcrypt==5.0.0             # Password hashing for student and admin accounts
cryptography==48.0.1      # Fernet encryption for stored admin credentials
numpy==2.5.1              # Native numerical runtime required by Matplotlib
matplotlib==3.11.1        # Headless student chart generation (Agg backend)
Pillow==12.3.0            # Matplotlib image support and IDE image validation
